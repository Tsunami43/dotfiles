# The packaged unit pins QT_QPA_PLATFORMTHEME=lxqt, so the file chooser it draws ignores the
# qt6ct palette every other Qt app on this desktop uses — including Telegram's own dialog.
# Point it at qt6ct so one palette covers them all.
[Service]
Environment="QT_QPA_PLATFORMTHEME=qt6ct"
